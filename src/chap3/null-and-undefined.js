var typeOfNull = typeof(null); //=> object
console.log(typeOfNull); 

var one;
var tyepOfUndefined = typeof(one);
console.log(tyepOfUndefined); // => undefined

var isEqual = (null == undefined); // => true
console.log(isEqual);

var isNotEqual = (null === undefined); // => false
console.log(isNotEqual);